allOf:
  - $ref: "#/components/schemas/RefreshTokenKey"
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - token
        properties:
          token:
            type: string
