allOf:
  - $ref: "#/components/schemas/AuthTokenKey"
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - access
        properties:
          access:
            type: string
