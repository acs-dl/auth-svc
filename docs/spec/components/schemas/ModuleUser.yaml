allOf:
  - $ref: "#/components/schemas/ModuleUserKey"
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - user_id
          - module_id
        properties:
          user_id:
            type: integer
            format: int64
          module_id:
            type: integer
            format: int64
